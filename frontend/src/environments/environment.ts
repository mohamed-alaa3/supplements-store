export const environment = {
  production: false,
  // Base URL of the real backend REST API (see BACKEND_FRONTEND_API_MAPPING.md).
  // All services call `${apiUrl}/...` — never hard-code a host anywhere else.
  apiUrl: 'http://localhost:5000/api'
};
